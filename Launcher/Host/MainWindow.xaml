<Window x:Class="StoryOfTimeLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:StoryOfTimeLauncher"
        mc:Ignorable="d"
        Title="Story of Time Launcher" Height="720" Width="1280"
    WindowStyle="None" ResizeMode="CanMinimize" AllowsTransparency="False" Background="#1a1a1a"
    WindowStartupLocation="CenterScreen">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0" />
    </WindowChrome.WindowChrome>
    <Grid>
        <wv2:WebView2 Name="webView" DefaultBackgroundColor="Transparent" />

        <!-- Update Overlay -->
        <Grid Name="UpdateOverlay" Visibility="Collapsed" Background="#1a1a1a" Panel.ZIndex="100">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Name="UpdateStatusText" Text="正在检查更新..." Foreground="White" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <ProgressBar Name="UpdateProgressBar" Width="400" Height="6" Visibility="Collapsed" Background="#333" Foreground="#EAB308" BorderThickness="0"/>
                <Button Name="UpdateConfirmButton" Content="立即更新" Visibility="Collapsed" Margin="0,20,0,0" Width="120" Height="35" 
                        Background="#EAB308" Foreground="Black" BorderThickness="0" Cursor="Hand" Click="UpdateConfirmButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
